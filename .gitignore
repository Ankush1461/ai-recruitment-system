# ── Secrets ───────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
.venv/
venv/

# ── Runtime data (regenerated by the app — never commit) ──────
recruiter.db
users.db
*.db-shm
*.db-wal
data/
chroma_db/
exports/
media/
.legacy_migrated
_interview_e2e.wav
*.log

# ── Editor / tooling ──────────────────────────────────────────
.vscode/
.idea/
.freebuff/
.DS_Store

# ── Downloaded helpers (cloudflared.exe from start-tunnel.bat) ──
tools/

# ── Archived dev artifacts (video production, transcription tests, probes) ──
# Kept locally for reference; never shipped with the app.
archive/
